# =============================================================================
# General Configuration
# =============================================================================

# Testing Target Selection
# Specify which system(s) to test: cribl, splunk, or both
# Valid values: cribl | splunk | both
TEST_TARGET="splunk"

# Number of test events to send per endpoint (default: 5)
NUM_EVENTS=5

# =============================================================================
# Cribl Configuration (Required if TEST_TARGET includes 'cribl')
# =============================================================================

# Cribl REST API Configuration
# For Cribl Cloud: Use https://api.cribl.cloud
# For Cribl Stream (self-hosted): Use https://your-cribl-instance:9000/api/v1
# Generate credentials:
#   - Cribl Cloud: https://manage.cribl.cloud → Settings → API Credentials
#   - Cribl Stream: Settings → API Credentials → Create New
CRIBL_API_URL="https://api.cribl.cloud"
CRIBL_CLIENT_ID="your-client-id-here"
CRIBL_CLIENT_SECRET="your-client-secret-here"

# Cribl HTTP Source Endpoint
# For Cribl Cloud: Use your cloud HTTP endpoint (e.g., https://<group-name>.<your-org-id>.cribl.cloud:<port>/services/collector)
# For Cribl Stream (self-hosted): Use http://your-cribl-instance:<port>/services/collector
CRIBL_HTTP_URL="https://<group-name>.<your-org-id>.cribl.cloud:<port>/services/collector"

# HEC token for Cribl event ingestion
# This token is tested to verify it can send events to Cribl
CRIBL_HEC_TOKEN="your-cribl-hec-token-here"

# Optional: Cribl worker group to check logs for (default: default)
# For distributed deployments, specify the worker group name
CRIBL_WORKER_GROUP="default"

# =============================================================================
# Splunk Configuration (Required if TEST_TARGET includes 'splunk')
# =============================================================================

# Splunk HEC Endpoint
# Base HEC URL - tool will test both /services/collector and /services/collector/raw
SPLUNK_HEC_URL="https://your-splunk-instance:8088/services/collector"

# HEC token for Splunk event ingestion
# This token is tested to verify it can send events to Splunk
SPLUNK_HEC_TOKEN="your-hec-token-here"

# Splunk Search API Configuration
# Management/Search API URL - typically https://your-splunk-instance:8089
SPLUNK_HTTP_URL="https://your-splunk-instance:8089"
SPLUNK_USERNAME="your-search-username"

# Splunk Authentication
# Use EITHER token (preferred) OR password - token is tried first if both provided
# IMPORTANT: If using SAML/SSO, password auth will NOT work - you MUST use SPLUNK_TOKEN
# Generate token in Splunk: Settings → Tokens → Create New Token
# Note: Use quotes if your password/token contains special characters (!@#$%^&* etc)
SPLUNK_TOKEN="your-splunk-bearer-token-here"
SPLUNK_PASSWORD="your-search-password"

# Optional: Override default index (leave empty to use Splunk default)
DEFAULT_INDEX=""

# =============================================================================
# Notes
# =============================================================================
# - Quotes are optional but recommended for values with special characters
# - After editing .env, just run 'python hec_yeah.py' - no need to reactivate venv
# - If TEST_TARGET=cribl or both, all Cribl parameters above are required
# - If TEST_TARGET=splunk or both, all Splunk parameters above are required
