# =============================================================================
# General Configuration
# =============================================================================

# Testing Target Selection
# Specify which system(s) to test:
#   - splunk: Test Splunk HEC directly (send to Splunk, verify in Splunk)
#   - cribl: Test Cribl connectivity (send to Cribl, verify HTTP response)
#   - cribl_to_splunk: Test pipeline (send to Cribl, verify in Splunk)
# Valid values: cribl | splunk | cribl_to_splunk
TEST_TARGET="splunk"

# Number of test events to send (default: 5)
NUM_EVENTS=5

# =============================================================================
# Cribl Configuration (Required for 'cribl' or 'cribl_to_splunk' modes)
# =============================================================================

# Cribl HEC Endpoint
# For Cribl Cloud: https://<workspaceName>.<organizationId>.cribl.cloud:<port>/services/collector
# For Cribl Stream (self-hosted): http://your-cribl-instance:<port>/services/collector
CRIBL_HEC_URL="https://<workspaceName>.<organizationId>.cribl.cloud:<port>/services/collector"

# Cribl HEC Token (optional)
CRIBL_HEC_TOKEN="your-cribl-hec-token-here"

# =============================================================================
# Splunk Configuration (Required for 'splunk' or 'cribl_to_splunk' modes)
# =============================================================================

# Splunk HEC Endpoint
SPLUNK_HEC_URL="https://your-splunk-instance:8088/services/collector"

# Splunk HEC Token
SPLUNK_HEC_TOKEN="your-hec-token-here"

# Splunk Management/Search API URL (port 8089, not 8088)
SPLUNK_HTTP_URL="https://your-splunk-instance:8089"

# Splunk Search Username
SPLUNK_USERNAME="your-search-username"

# Splunk Bearer Token (preferred - for search API authentication)
# Generate in Splunk: Settings → Tokens → Create New Token
SPLUNK_TOKEN="your-splunk-bearer-token-here"

# Splunk Password (alternative to token - not recommended for SAML/SSO)
# Use quotes if password contains special characters (!@#$%^&* etc)
SPLUNK_PASSWORD="your-search-password"

# Optional: Override default index (leave empty to use Splunk default)
DEFAULT_INDEX=""

# =============================================================================
# Notes
# =============================================================================
# - Quotes are optional but recommended for values with special characters
# - After editing .env, just run 'python hec_yeah.py' - no need to reactivate venv
# - SPLUNK_HTTP_URL must include port 8089 (management API), not 8088 (HEC)
# - Configuration requirements based on TEST_TARGET:
#     splunk:           Only Splunk parameters required
#     cribl:            Only CRIBL_HEC_URL required (token optional)
#     cribl_to_splunk:  Both Cribl and Splunk parameters required
